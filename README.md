# Retention рекламных пользователей и анализ провала DAU

Кейс из продуктовой аналитики:  
анализ удержания пользователей, пришедших из рекламной кампании, и разбор однодневного провала метрики DAU в ленте новостей мобильного приложения.

Стек: **ClickHouse + Apache Superset**.

---

## 🎯 Задачи

1. **Retention рекламных пользователей**
   - Оценить поведение пользователей, пришедших по рекламной кампании (`source = 'ads'`).
   - Понять, как быстро они отваливаются и какая доля остаётся активной через 1, 7, 14+ дней.

2. **Провал DAU**
   - Зафиксирован однодневный провал DAU.
   - Нужно ответить:
     - кто именно не смог пользоваться лентой в этот день;
     - объединяющие признаки (операционная система, тестовая группа `exp_group`, страна).

Источники данных (хранилище на ClickHouse):

- таблица событий ленты: `feed_actions`
- таблица событий сообщений: `message_actions`


---

## 📊 Что построено в Superset

### 1. Блок: динамика DAU и эффект рекламы

**Графики:**

1. **DAU ленты по дням**  
   Метрика: `DAU = COUNT(DISTINCT user_id)` по дате.  
   Показывает общий уровень дневной активности и точки аномалий.  
   ![DAU feed by day](images/dau_overall.png)

2. **DAU по источникам (ads vs organic)**  
   Метрика: DAU по `source` (`ads`, `organic`).  
   ![DAU by source](images/dau_by_source.png)

**Инсайт:**

- хорошо видно:
  - день крупной рекламной кампании (резкий всплеск `ads`);
  - отдельный день провала DAU, не связанный напрямую с кампанией.

---

### 2. Блок: Retention рекламной когорты

Когорта: пользователи, у которых **первый день в ленте** = `2025-11-08` и `source = 'ads'`.

#### 2.1. Тепловая карта когортного ретеншена

**Чарт:** `Когортный Retention ленты по дням, % — реклама (ads)`  

- X: `day_number` — номер дня жизни когорты (0, 1, 2, …)
- Y: `start_date` — дата когорты
- Метрика: `retention_pct` = доля активных пользователей когорты в %.

Для анализа используется диапазон когорт **с 2025-11-01 по 2025-11-15**, отдельно через фильтр выделяется когорта `2025-11-08` для кампании.

![Ads retention heatmap](images/ads_retention_heatmap.png)

#### 2.2. Табличный срез по рекламной когорте 8 ноября

**Таблица:** `Рекламная когорта 8 ноября — Retention по дням`  

Колонки:

- `day_number` — день жизни когорты (0 = 8 ноября),
- `date` — календарная дата,
- `active_users` — количество активных в ленте пользователей когорты,
- `retention_pct` — доля от исходного размера когорты.

![Ads retention table](images/ads_retention_table.png)

**Ключевые наблюдения (реальные цифры берутся из этой таблицы):**

- размер рекламной когорты: ~2.6k пользователей;
- уже на **1-й день** удержание ≈ 4%;
- далее ретеншен быстро падает до 1–2% и держится на этом уровне;
- на горизонте ~2–3 недель в ленту возвращается менее 2% рекламной когорты.

---

### 3. Блок: анализ провала DAU

#### 3.1. Разрез по операционным системам

**График:** `DAU по ОС: день до провала и день провала`  

- метрика: DAU по `os` (Android / iOS);
- сравниваются две даты: день до провала и день провала.

![DAU by OS, before vs drop](images/dau_os_drop.png)

**Суть:**

- DAU падает и у пользователей **Android**, и у пользователей **iOS**;
- относительное снижение примерно одинаковое (порядка −12–15%).

Это сигнал, что проблема вряд ли привязана только к одной ОС или конкретной версии клиента.

---

#### 3.2. Разрез по ОС и exp_group

**Графики:**

1. `DAU по ОС и exp_group: день до провала`  
   ![DAU by OS & exp_group, before](images/dau_os_exp_before.png)

2. `DAU по ОС и exp_group: день провала`  
   ![DAU by OS & exp_group, drop](images/dau_os_exp_drop.png)

Разбиение:

- X: `exp_group` (0–4),
- цвет: `os` (Android / iOS),
- метрика: DAU.

**Суть:**

- во всех `exp_group` внутри каждой ОС видно одинаковое снижение;
- нет «провалившейся» одной экспериментальной группы — падают все группы вместе.

Это снижает вероятность того, что причина в конкретном A/B-тесте или частичной раскатке фичи.

---

#### 3.3. Разрез по странам

Чтобы проверить гипотезу про географию, строится:

**График:** `DAU по странам (топ-5): день до провала и день провала`  

- выбираются TOP-5 стран по DAU за день до провала;
- сравнивается DAU этих стран в день до и в день провала.

![DAU by country](images/dau_country_top5.png)

**Суть:**

- основное снижение DAU приходится на ключевой рынок (Россия);
- по другим странам изменения значительно меньше.

Комбинация всех трёх разрезов (OS, exp_group, страна) даёт историю:  
падение не связано с конкретной ОС или экспериментом и концентрируется в основном регионе → выглядит как инфраструктурный/сетевой инцидент на стороне сервера.

---

## 🧠 Выводы (кратко)

1. **Рекламный трафик даёт всплеск, но слабое удержание.**
   - Одноразовый рост DAU за счёт кампании.
   - Очень быстрый отток: на 1-й день остаётся около 4%, дальше — узкое ядро в 1–2%.

2. **Провал DAU не привязан к одной платформе или тесту.**
   - Падение и на Android, и на iOS.
   - Все `exp_group` падают синхронно → эксперимент — не основной кандидат на причину.

3. **Основной удар приходится на базовый регион.**
   - Сильнее всего проседает DAU в основной стране.
   - Это больше похоже на инфраструктурный инцидент (сервер/сеть) в регионе, а не на продуктовую гипотезу.

---

## 🛠 Технологии

- **Хранилище:** ClickHouse (схема `simulator_20251120`)
- **BI-инструмент:** Apache Superset
- **Язык запросов:** SQL (ClickHouse)

Все SQL-запросы, используемые для построения датасетов и графиков, находятся в файле:

```text
sql/feed_retention_and_dau_drop.sql
